def shell
 let _lines $0
 let _args $1  / top level args, not used
 let _i 0
 len $_lines _len
 #loop
  jeq $_i $_len done
  get $_lines $_i _line
  cal execute_cmd $_line
  add _i $_i 1
 jmp loop
 #done
end